import React from 'react';
import { AMachine, CMachine } from './pages';

export function App() {
    return (
        <React.Fragment>
            <AMachine/>
            <CMachine />
        </React.Fragment>
    );
};
